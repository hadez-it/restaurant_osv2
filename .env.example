# Postgres connection string (e.g. from Neon / Vercel Marketplace)
DATABASE_URL="postgresql://postgres:devpass@localhost:5432/restaurant"
# Secret for signing auth tokens — set a long random string in production
JWT_SECRET="change-me"
